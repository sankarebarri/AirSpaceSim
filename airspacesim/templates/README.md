# Static Template Compatibility

These templates are retained for the library/offline `airspacesim init` workflow and static compatibility demos.

New hosted UI work should not be added here. Use `apps/web/src/` for the React application.

See `docs/architecture/legacy_static_ui_decision.md` for the current legacy static UI decision.
