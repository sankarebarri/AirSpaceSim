# Static Asset Compatibility

These static assets are retained for the library/offline `airspacesim init` workflow and static compatibility demos.

New hosted UI assets should not be added here. Use `apps/web/src/` and `apps/web/public/` for the React application.

See `docs/architecture/legacy_static_ui_decision.md` for the current legacy static UI decision.
