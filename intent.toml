[intent]
schema_version = 1

[python]
version = "3.10"

[commands]
test = "pytest -q"
lint = "ruff check ."

[ci]
install = "-e . && python -m pip install -r requirements-dev.txt"
python_versions = ["3.10", "3.11", "3.12"]
triggers = ["push", "pull_request", "workflow_dispatch"]

[policy]
pack = "default"
strict = false
